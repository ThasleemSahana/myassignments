const browserName = "Chrome" ;
function getBrowserName(){
    if (browserName === 'Chrome');
    {
        var browserName = "firefox" ;
         console.log ("browser name is printed inside block", browserName);
    }
 let browserNamel = "firefox" 
    console.log ("browser name is printed outside block", browserNamel);

}
getBrowserName() ;